#mobile no problm
import re

txt = "my number is 9341924690 and I will come at 6:00"
x = re.findall("\d{10}", txt)
print(x)




#first and last letter name matching prblm
import re

frnds=input("Enter your friends: ")

word= frnds.split()
for word in words:

    x = re.search("^a.*a$",word, re.IGNORECASE)
    if x:
        print("YES! The word matches!")
    else:
        print("No match for the word!")



